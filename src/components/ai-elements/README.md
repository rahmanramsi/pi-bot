# AI Elements provenance

These source-owned components were installed from the official AI Elements registry with `ai-elements@1.9.0` (`npx ai-elements@1.9.0 add conversation message prompt-input tool code-block task`) and adapted to Pi Bot's local Electron/TimelineItem contract.
